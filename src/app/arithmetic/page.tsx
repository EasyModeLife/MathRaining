import ArithmeticTrainer from "@modules/arithmetic/ui/Trainer";

export const metadata = { title: "Aritmética — MathRaining" };

export default function ArithmeticPage() {
  return <ArithmeticTrainer />;
}
